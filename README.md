**ForgeLang** is a dynamically typed language with an interpreter (WIP compiler) written in python. For efficiency reasons, the repo is deprecated, and will be replaced by a 
C++ implementation in the future.
